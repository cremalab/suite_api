require 'test_helper'

class CommentsControllerTest < ActionController::TestCase
  test "index" do
    assert false, "Hasn't been written"
  end

  test "create" do
    assert false, "Hasn't been written"
  end

  test "show" do
    assert false, "Hasn't been written"
  end

  test "update" do
    assert false, "Hasn't been written"
  end

  test "destroy" do
    assert false, "Hasn't been written"
  end

end
